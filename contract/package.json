{
  "name": "contract",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prestart": "asb && near deploy",
    "dev:deploy:contract": "near dev-deploy ./build/release/contract.wasm",
    "deploy:contract": "asb && near deploy",
    "build": "asb"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "near-cli": "^2.1.0",
    "near-sdk-as": "^3.2.3"
  }
}
